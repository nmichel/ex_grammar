# If you want to see logs in your test (for debugging for example),
# remove the `capture_log: true` option.
# Then set the level in your test with `Logger.configure(level: :debug)`
ExUnit.start(capture_log: true)
